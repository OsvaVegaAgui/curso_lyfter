lista = [
    'Osvaldo',
    'Jorge',
    'Javier',
    'Joaquin'
]

# for nombre in lista:
#     nombre = nombre.upper()
    
for index in range(0,len(lista)):
    lista[index] = lista[index].upper()

for nombre in lista:
    print(nombre)